python_repl: python3

python_script:
  prog: python3
  args: [-c]
  cmd: |
    fruits = ["apple", "banana", "watermelon"]
    for x in fruits:
      print(f"{x} is a fruit with {len(x)} characters!")
