on:
  push:
    branches:
      - "*"
  pull_request:
    types: [opened, reopened, synchronize]

name: Push

jobs:
  lint:
    name: Lint
    uses: ./.github/workflows/lint.yml
  test:
    name: Test
    uses: ./.github/workflows/test.yml
